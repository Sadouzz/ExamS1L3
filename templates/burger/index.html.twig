{% extends 'base.html.twig' %}

{% block body %}
    <main class="main" id="main">
        <div class="pagetitle">
            <h3>Liste des burgers</h3>
            <nav>
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">Accueil</li>
                    <li class="breadcrumb-item active" aria-current="page">Burgers</li>
                </ol>
            </nav>
        </div>
        <div class="card mb-3 shadow border-0 rounded-0">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h6 class="mb-0">
                        <i class="bi bi-funnel-fill text-secondary me-2"></i>
                        Filtres et recherche
                    </h6>
                </div>

                {{ form_start(formSearchBurger) }}
                <div class="row g-3">
                    <div class='col-lg-3 col-md-12'>
                        {{ form_row(formSearchBurger.burgerCategorie) }}
                    </div>
                    <div class='col-lg-2 col-md-12'>
                        {{ form_row(formSearchBurger.isArchived) }}
                    </div>
                    <div class='col-lg-1 col-md-12 align-content-end'>
                        <button class="btn btn-secondary">Filtrer</button>
                    </div>
                </div>
                {{ form_end(formSearchBurger) }}
            </div>
        </div>

        <div class="card shadow border-0 rounded-0">
            <div class="p-3 border-bottom">
                <div class="d-flex justify-content-between align-items-center">
                    <h5 class="card-title m-0">
                        Liste des burgers
                        <span class="badge bg-info ms-2">26</span>
                    </h5>
                </div>
            </div>
            <table class="table table-striped mb-0">
                <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Image</th>
                    <th>Nom</th>
                    <th>Prix</th>
                    <th>Categorie</th>
                    <th>Description</th>
                    <th>Statut</th>
                </tr>
                </thead>
                <tbody>

                {% for burger in burgers %}
                    <tr>
                        <td>{{ burger.id }}</td>
                        <td>
                            <img src="{{ burger.imageUrl }}" alt="{{ burger.libelle }}" style="max-width: 50px; max-height: 50px;">
                        </td>
                        <td>{{ burger.libelle }}</td>
                        <td>{{ burger.prix|number_format(0, ',', ' ') }} FCFA</td>
                        <td>{{ burger.categorieName }}</td>
                        <td>{{ burger.description }}</td>
                        <td>
                            {% if burger.isArchived %}
                                <span class="badge bg-danger">Archivé</span>
                            {% else %}
                                <span class="badge bg-success">Non Archivé</span>
                            {% endif %}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7" class="text-center text-muted py-4">
                            Aucun burger trouvé
                        </td>
                    </tr>
                {% endfor %}

                </tbody>
            </table>
        </div>
    </main>
{% endblock %}
